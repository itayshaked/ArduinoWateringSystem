version: 1.0
runtime: nodejs12
build:
  commands:    
    build:
       npm install                                  
run:                              
  command: node dynamoAccess.js
  network:
    port:5000
