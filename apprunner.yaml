version: 1.0
runtime: nodejs12
build:
  commands:    
    build:
       npm install                                  
run:                              
  command: npm run start
  network:
    port:5000
