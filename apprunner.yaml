version: 1.0
runtime: nodejs12
build:
  commands:    
    build:
       npm install                                  
run:                              
  command: npm start-server
  network:
    port:5000
